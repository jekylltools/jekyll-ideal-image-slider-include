# Config settings
title: Jekyll Ideal Image Slider Include Demo
baseurl: "/jekyll-ideal-image-slider-include"

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
